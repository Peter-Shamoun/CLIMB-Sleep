program: train.py
name: scl-grid-sweep-2
project: sleep-consolidated-learning
method: grid
metric:
  goal: minimize
  name: eval/best_perplexity_mean

parameters:
  experiment.seed:
    values: [1, 2, 3]
  sleep_mechanism.replay_ratio:
    values: [0.05, 0.2, 0.8]
  sleep_mechanism.n_phases:
    values: [50, 100, 200]
  sleep_mechanism.sleep_wake_ratio:
    values: [10.0, 20.0, 40.0]
  # sleep_mechanism.contextualize_sleep:
  #   values: [True, False]

command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}
