# Multi-seed Nautilus Job for the matched-budget CLM epoch baseline
# (sleep_mechanism=baseline_like_clm, 343,940 steps = 10 equivalent epochs).
# Each completion index is one seed (SEED = index + 1); run names
# baseline_clm_<seed>, W&B group baseline-clm. Mirrors sh_arms_job.yaml.
# Placeholders: __SEEDS__, __HF_READ_TOKEN__, __HF_WRITE_TOKEN__,
# __WANDB_API_KEY__. Never commit a rendered copy.
#   kubectl create -f rendered.yaml -n dsc-capstone-25-26
#   kubectl get pods -n dsc-capstone-25-26 -l job-name=sh-baseline-clm-001
apiVersion: batch/v1
kind: Job
metadata:
  name: sh-baseline-clm-001
spec:
  completions: __SEEDS__
  parallelism: __SEEDS__
  completionMode: Indexed
  backoffLimit: 20
  ttlSecondsAfterFinished: 86400
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: gpu-container
        image: pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime
        env:
        - name: HF_READ_TOKEN
          value: "__HF_READ_TOKEN__"
        - name: HF_WRITE_TOKEN
          value: "__HF_WRITE_TOKEN__"
        - name: HUGGINGFACE_HUB_TOKEN
          value: "__HF_READ_TOKEN__"
        - name: HF_HOME
          value: /mnt/data/huggingface
        - name: WANDB_API_KEY
          value: "__WANDB_API_KEY__"
        - name: GROUP
          value: "baseline-clm"
        command: ["/bin/bash", "-c"]
        args:
        - |
          set -e
          echo "Pod started with PyTorch + CUDA"
          apt-get update --fix-missing && apt-get install -y --no-install-recommends git unzip
          git clone https://github.com/Peter-Shamoun/CLIMB-Sleep.git
          cd CLIMB-Sleep
          git checkout main_refactor
          git log --oneline -1
          ./setup.sh /mnt/data/evaluation_data.zip
          . .venv/bin/activate
          export PATH="$(pwd)/.venv/bin:$PATH"
          which python
          nvidia-smi --query-gpu=name,memory.total --format=csv,noheader || true
          export SEED=$((JOB_COMPLETION_INDEX + 1))
          export RUN_NAME=baseline_clm_${SEED}
          echo "Run: $RUN_NAME (seed=$SEED)"
          python train.py sleep_mechanism=baseline_like_clm task=base_clm model=gpt2_small dataset=strict \
            experiment.seed=$SEED experiment.name=$RUN_NAME experiment.group=$GROUP
          echo "Finished Training!"
        volumeMounts:
        - mountPath: /mnt/data
          name: storage-volume
        resources:
          limits:
            nvidia.com/gpu: "1"
            memory: 8G
            cpu: "2"
          requests:
            nvidia.com/gpu: "1"
            memory: 8G
            cpu: "2"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - k8s-gpu-01.calit2.optiputer.net
                - k8s-chase-ci-01.calit2.optiputer.net
                - k8s-chase-ci-02.calit2.optiputer.net
                - k8s-chase-ci-03.calit2.optiputer.net
                - k8s-chase-ci-04.calit2.optiputer.net
                - k8s-chase-ci-05.calit2.optiputer.net
                - k8s-haosu-15.sdsc.optiputer.net
                - k8s-haosu-18.sdsc.optiputer.net
                - k8s-haosu-11.sdsc.optiputer.net
                - k8s-ravi-01.calit2.optiputer.net
                - chi-dgx-node02.csuchico.edu
                - fiona8-1.calit2.uci.edu
                - nautilus-ext-gpu01.fullerton.edu
                - hcc-chase-shor-c4705.unl.edu
                - k8s-chase-ci-07.calit2.optiputer.net
                - hcc-nrp-shor-c6017.unl.edu
      volumes:
      - name: storage-volume
        persistentVolumeClaim:
          claimName: scl-replay-experiments-2
